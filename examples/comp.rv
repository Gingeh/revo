const a = comp macro do
	"1 + 1"
end

# print(a)

const x = comp do
	let res = input():unwrap()
		|> fn(c) c:upper()
	match res
	| "HELLO" when input()?:len() < 2 =>
    set_metatable({}, {
			__index = fn(self, k) k * 2
		})
	| x when do
		let u = x
		print(u)
	end => :ok
end

print(x)
