struct Version {
	major: number,
	minor: number,
	fn is_beta(self) self.major < 1,
	fn of_string(str) do
		const res = str:split(".")
			|> iter.map(fn(c) number(c))
			|> iter.collect()

		return (:ok, Version{major = res[0]?, minor = res[1]?})
	end,
	fn to_string(self) fmt("%v.%v", self.major, self.minor)
}

const v = Version.of_string("1.2.3")
print(v)
